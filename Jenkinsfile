pipeline {
    agent { dockerfile true }
    tools {
        maven 'Maven 3.6.0'
        jdk 'JDK 1.8'
    }
    stages {
        stage('Build') {
            steps {
                sh 'ls'
            }
        }
    }
}